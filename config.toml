base_url = "https://seri.tools"

compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

title = "seri.tools"
author = "Dennis Duda (seri)"

minify_html = true

generate_feeds = true
feed_filenames = ["rss.xml", "atom.xml"]

[markdown]
highlight_code = true
extra_syntaxes_and_themes = ["sass"]
highlight_theme = "gruvbox-material"
bottom_footnotes = true

[extra]
